{
  "name": "hep",
  "private": true,
  "version": "0.1.0",
  "description": "Harmony Events & Platform — India's end-to-end event management PWA",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview",
    "dev:api": "pnpm --filter api dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  }
}
